🚀Create an HTML page with a number display (start at 0) and two buttons
✅Select the display element and buttons using querySelector
🖱️Add click event listener to the "increase" button that increases the number by 1
🖼️Update the display to show the new number
🖱️Do the same for "decrease" button (subtract 1)
Add a "reset" button that sets it back to 0
